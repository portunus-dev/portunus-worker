{
  "name": "portunus-worker",
  "version": "1.0.0",
  "description": "package for creating workers templates",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,jsone,md}'",
    "lint": "eslint *.js modules/*.js",
    "dev": "env $(print-env) npx wrangler dev"
  },
  "author": "Runzhou Li (woozyking) <runzhou.li@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/wrangler": "^1.19.5",
    "eslint": "^8.5.0",
    "prettier": "^2.2.1"
  },
  "dependencies": {
    "deta-worker": "^0.2.0-alpha",
    "itty-router": "^2.4.5",
    "jsonwebtoken": "^8.5.1",
    "serverless-cloudflare-workers": "^1.2.0"
  }
}
